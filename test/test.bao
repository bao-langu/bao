hàm chính() -> Z32
    biến a E Z32 := 3
    hằng b E R64 := 3.4
    hằng MỘT E Z32 := 1
    a := MỘT + MỘT
    biến c E Z64
    biến d E Z32 := 3
    trả về (1 + a) * 2 / d - 2
kết thúc

thủ tục test()
    trả về
kết thúc