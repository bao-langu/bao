
hàm chính() -> Z32
    biến a E Z32 := 3

    hằng MỘT E Z32 := 1
    hằng KHÔNG E Z32 := 0
    hằng b E R32 := 3.4
    
    a := MỘT + MỘT

    biến c E Z64
    biến d E Z32 := 3

    trả về (1 + a) * 2 / d - (d - 1)
kết thúc

thủ tục test()
    trả về
kết thúc